Voila il faut encore bien evidemment mettre dans les cases ce qu'on veut que les actions fassent ! 
en gros ce que tu me disait, droite -> allez a droite
gauche -> allez a gauche
bs -> descendre la piece
haut -> tourner la piece ! 


//Librairie standard
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
//Pour utiliser la SDL
#include <SDL/SDL.h>

int main(int argc, char *argv[])

{

  SDL_Surface *ecran = NULL;
  SDL_Event event; /* La variable contenant l'événement */
  int continuer = 1; /* Notre booléen pour la boucle */
  SDL_Init(SDL_INIT_VIDEO);
  ecran = SDL_SetVideoMode(640, 480, 32, SDL_HWSURFACE);
  SDL_WM_SetCaption("Gestion des événements en SDL", NULL);

  while (continuer)

    {

      SDL_WaitEvent(&event);

      switch(event.type)

	{
      
        case SDL_KEYDOWN:
	  switch (event.key.keysym.sym)
            {
	    case SDLK_ESCAPE: /* Appui sur la touche Haut alors...*/
	      continuer = 0;
	      break;

	    case SDLK_UP: /* Appui sur la touche Haut alors...*/
	      
	      break;
	    case SDLK_DOWN: /* Appui sur la touche Bas alors...*/
	      
	      break;
	    case SDLK_RIGHT: /* Appui sur la touche Droite alors ...*/
	      
	      break;
	    case SDLK_LEFT: /* Appui sur la touche Gauche alors ...*/
	      
	      break;

            }
	  break;
	}
    }

  SDL_Quit();

  return EXIT_SUCCESS;

}
